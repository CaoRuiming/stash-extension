{
  "name": "stash",
  "version": "1.0.0",
  "description": "Stashes URLs for later.",
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Raymond Cao",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "0.0.179",
    "typescript": "^4.6.2"
  }
}
